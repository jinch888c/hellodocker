version: '1.0'
steps:
  BuildingDockerImage:
    title: Building Docker Image
    type: build
    image_name: hellodocker
    working_directory: ./
    tag: '${{CF_BRANCH_TAG_NORMALIZED}}'
    dockerfile: Dockerfile
  deploy:
  image: 'codefresh/cfstep-helm:2.9.1'
  environment:
    - CHART_REF=hello-svc
    - RELEASE_NAME=hellosvc-prod
    - KUBE_CONTEXT=game
